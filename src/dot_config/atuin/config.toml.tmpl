# Network
sync_address = "{{ .atuinSyncAddress }}"
update_check = false

# Appearance
inline_height = 15
style = "compact"
history_format = "{time} {command}"

# Search and filter
search_mode = "skim"
history_filter = ["^;.*"]

# Misc
enter_accept = false
