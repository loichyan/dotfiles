{
  "dns": {
    "hosts": {
      {{ range $k, $v := .proxy.dnsHosts }}
      {{ printf "%s: %s," (toJson $k) (toJson $v) }}
      {{ end }}
      "dns.alidns.com": ["223.5.5.5", "223.6.6.6"],
      "doh.pub": ["1.12.12.12", "120.53.53.53"]
    },
    "servers": [
      {
        "address": "https://dns.alidns.com/dns-query",
        "domains": ["geosite:cn"]
      },
      {
        "address": "https://doh.pub/dns-query",
        "domains": ["geosite:cn"]
      },
      "fakedns",
      "localhost"
    ]
  }
}
